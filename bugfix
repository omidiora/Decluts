
PREVEIW PAGE
 Preview page(When a user clicks on a product is not yet completed ... look disorderly and not like the design)

AddItem 3 (The name of the item should use the general name,  it should show Mb also ,  also it show the 
background card)

 Upload All Item (This is when a user is meant to upload an image(and item) it does not look exactly like the design)

Option should be moved to additem 1 from additem2


change the button color on disabled when  user as not fill in all the required on uploading


Once a user upload a product .. it should show automatically

